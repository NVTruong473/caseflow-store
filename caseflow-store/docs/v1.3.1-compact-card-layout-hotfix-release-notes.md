# CaseFlow Books v1.3.1 Compact Card Layout Hotfix Release Notes

- Date: 2026-07-18
- Status: production deployed and smoke tested as `v1.3.1`
- Production URL: `https://caseflow-store.vercel.app`
- Vercel deployment: `dpl_CtyPPR1cExwXQWctsh7to98Vg3yb`
- Deployment URL: `https://caseflow-store-n7qc7ggxh-nvt-ruong473.vercel.app`
- Inspect URL: `https://vercel.com/nvt-ruong473/caseflow-store/CtyPPR1cExwXQWctsh7to98Vg3yb`

## What Changed

- Fixed compact book-cover card layout overlap in related-book recommendation
  cards on product detail pages.
- Updated homepage hero compact cards and shelf compact cards that shared the
  same fixed-column risk.
- Added a Playwright verifier that measures cover/content bounding boxes across
  mobile, tablet, and desktop viewports instead of relying only on viewport
  overflow checks.

## What Did Not Change

- No new user-facing feature was added.
- No database schema, production data import, payment/shipping provider,
  account-verification provider, catalog source, or API contract changed.
- The existing `v1.3.0` tag and GitHub Release remain historical release
  records; this hotfix should be released as `v1.3.1`.

## Verification Summary

- Local compact-card overlap verifier passed against a production build.
- Production compact-card overlap verifier passed against
  `https://caseflow-store.vercel.app`.
- Affected v1.3 homepage and book-detail visual hierarchy verifiers passed.
- Production release smoke passed with 100 active editions, 100 cover responses,
  50 English editions, 50 Vietnamese editions, and public/account/admin
  boundary checks passing.
- `npx tsc --noEmit --pretty false`, `npm run lint`, `npm run build`,
  `npx tsx scripts/verify-release-cleanup.ts`, targeted secret-value scan,
  `npm audit --audit-level=high`, and `git diff --check` passed.

## Residual Notes

- The first broad production smoke attempt timed out waiting for a seeded cart
  count. A direct cart probe passed and the full smoke retry passed, so this is
  documented as a timing flake, not an open cart defect.
- `npm audit --audit-level=high` passes; the known moderate Next/PostCSS
  advisory remains because the suggested forced fix would downgrade Next.js in a
  breaking way.
